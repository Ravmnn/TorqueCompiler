package Geometry;


# same package ^, same package definitions (functions, structures, interfaces...)
# no need to "unpack Geometry" in order to access "Vector"




int main()
{
    let squareVertices = Vertex[4] { ... };
    let square = Mesh::new(squareVertices);

    

    return 0;
}