int main(int argc, char** argv)
{
    char* response;
    int age = 15;

    if (age < 12)
        response = char array[3] { 'k', 'i', 'd' };
    else if (age < 16)
        response = char array[4] { 't', 'e', 'e', 'n' };
    else
        response = char array[5] { 'a', 'd', 'u', 'l', 't' };

    return 0;
}